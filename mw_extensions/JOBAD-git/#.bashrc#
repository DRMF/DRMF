# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions

alias ls="ls -aFv"
alias grad="ssh -Y grad.cam.nist.gov"
alias php="php --server localhost:8000 --docroot /home/hcohl/devel/mediawiki-1.20.5/"

